{
  "name": "campaign-registry-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base": "hardhat run scripts/deploy.ts --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5",
    "dotenv": "^16",
    "hardhat": "^2.22.0",
    "typescript": "^5"
  }
}
